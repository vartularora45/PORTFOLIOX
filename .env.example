# Server Configuration
PORT=5000

# Email Configuration (SMTP)
# For Gmail: Use App Password (not regular password)
# For SendGrid: Use API key as password
# For other providers: Check their SMTP documentation

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Recipient email (where contact form submissions go)
RECIPIENT_EMAIL=alex.chen@example.com

# Production Notes:
# 1. Never commit this file with real credentials
# 2. Use your hosting platform's environment variable system
# 3. For Gmail: Enable 2FA and use App Passwords
# 4. For production: Consider using SendGrid, Mailgun, or AWS SES
